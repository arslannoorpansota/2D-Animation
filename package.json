{
  "name": "2D",
  "version": "1.0.0",
  "description": "",
  "main": "linear.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development postcss src/styles.css -o ./dist/tailwind.css -w",
    "build": "NODE_ENV=production postcss src/styles.css -o ./dist/tailwind.css && cp -r ./src ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.1.2"
  }
}
